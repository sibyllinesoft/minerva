version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: metamcp
      POSTGRES_USER: metamcp
      POSTGRES_PASSWORD: metamcp_dev
      # Optimized for laptop development
      POSTGRES_SHARED_BUFFERS: 256MB
      POSTGRES_WORK_MEM: 16MB
      POSTGRES_EFFECTIVE_CACHE_SIZE: 1GB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init_db.sql:/docker-entrypoint-initdb.d/init_db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U metamcp -d metamcp"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - metamcp

  meta-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://metamcp:metamcp_dev@postgres:5432/metamcp
      CONFIG_PATH: /app/config/development.yaml
      LOG_LEVEL: INFO
      PYTHONPATH: /app
    volumes:
      - ./app:/app/app
      - ./config:/app/config
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - metamcp
    profiles:
      - full

  # Redis for caching (optional, for production)
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - metamcp
    profiles:
      - full

volumes:
  postgres_data:
  redis_data:

networks:
  metamcp:
    driver: bridge